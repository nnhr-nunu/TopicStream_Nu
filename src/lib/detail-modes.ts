import { DETAIL_LABEL_MAX } from "@/lib/constants";
import { isGenericAngle, topicAnchor } from "@/lib/mock-topics";
import type { BoardMode } from "@/lib/types";

/**
 * 「具体的にする」: カードの切り口に対して、具体的な話題・対応策・企画案・行動などを短い文で 8 つ出す。
 * 展開の形（3×3）はふつうの広げ方と同じ。出した答えのカードも、そのまま広げられる。
 */
export type DetailPreset = {
  /** AI に頼む中身 */
  ask: string;
  /** 1つの答えの文字数の目安（最小・最大） */
  length?: [number, number];
  rules: string[];
  /** キー無し・失敗したときの答え。{seed} をカードの文に置き換える */
  offline: string[];
};

export const DETAIL_PRESETS: Record<BoardMode, DetailPreset> = {
  chat: {
    ask: "このお題をもっと具体的にした話題（具体的なもの・場面・エピソードの種類）",
    // ふつうのカードと同じ長さにして、そのまま広げたり図鑑の候補にしたりできるようにする
    length: [5, 16],
    rules: [
      "体言止めの短い語句にする（例: お題「夜食」なら「深夜のカップ麺アレンジ」「コンビニの新作ホットスナック」）",
      "カテゴリの名前だけでなく、聞いた人が自分の体験をすぐ思い出せる具体的なもの・場面にする",
      "定番の話題と、ちょっと意外な話題を半分ずつ混ぜる",
      "文や問いかけにしない",
    ],
    offline: [
      "{seed}の最近の出来事",
      "{seed}の失敗談",
      "{seed}のあるある",
      "{seed}の昔と今",
      "{seed}の人に言えない本音",
      "{seed}のおすすめベスト3",
      "リスナーの{seed}体験",
      "{seed}のこだわり",
      "{seed}の初めての思い出",
      "{seed}の地域差",
    ],
  },
  advice: {
    ask: "相談への具体的な対応策（今日からできる行動、考え方の切り替え、気持ちの整え方、人への頼り方、状況の変え方）",
    length: [10, 30],
    rules: [
      "メモの箇条書きのように、「〜してみる」「〜を試す」「〜と決める」の言い切りで終える",
      "1つの答えに行動は1つだけ。何を・いつ・どれだけが分かるように書く（例: 「寝る前に、今日できたことを1つだけ書く」）",
      "今日すぐできる小さな行動・考え方の切り替え・人や窓口に頼る・環境を変える、を混ぜる",
      "「〜しよう」「〜はどう？」「〜だよ」のような語りかけ・励ましの言い方はしない",
      "お題の言葉を毎回くり返さない。説教・断定・診断はしない。本人を責めない",
      "命や健康に関わる深刻な内容なら、1つは「信頼できる人や専門の窓口に話す」を入れる",
    ],
    offline: [
      "「{seed}」で思っていることを、紙に3行だけ書き出してみる",
      "信頼できる人に、一言だけ話してみる",
      "考えるのは1日10分まで、と時間を決める",
      "自分で変えられることを、1つだけ選んで試す",
      "少し楽だった日の共通点を、手帳から探してみる",
      "今週中に答えを出さなくていい、と決めておく",
      "友達が同じことで悩んでいたら、何と声をかけるか考える",
      "まず睡眠と食事を、いつもより少し整える",
      "つらさが続くなら、専門の相談窓口に話してみる",
      "小さな一歩を、明日の予定に書き込む",
    ],
  },
  idea: {
    ask: "そのまま実行できる具体的なアイデア・企画案そのもの（進め方の助言ではなく、中身）",
    length: [8, 28],
    rules: [
      "企画名のような体言止めか「〜する」の形にし、聞いただけで何をするか分かるようにする（例: お題「雑談配信の新企画」なら「リスナーの黒歴史を匿名で朗読する回」）",
      "定番を少しひねった案と、意外な組み合わせ・逆転の発想の案を混ぜる",
      "誰に向けたものか・何が面白いのかが伝わる言葉を入れる",
      "「〜を考える」「〜を検討する」のような、中身の無い進め方の言葉にしない",
    ],
    offline: [
      "{seed}をリスナー参加型にする",
      "{seed}を1分で説明するチャレンジ",
      "{seed}をランキング形式で語る回",
      "{seed}を初心者向けの入門回にする",
      "{seed}の縛りルール付きバージョン",
      "{seed}をクイズ形式にしてみる",
      "{seed}を逆の立場からやってみる",
      "{seed}のビフォーアフター企画",
      "{seed}を毎週の連載コーナーにする",
      "{seed}の失敗談だけを集める回",
    ],
  },
  goal: {
    ask: "目標に近づく具体的な行動（いつ・何を・どれだけ・どう続けるか）",
    length: [10, 30],
    rules: [
      "「〜する」で言い切り、回数・時間・期限などの数字を1つ以上入れる（例: 「毎朝7時に、単語を10個だけ覚える」）",
      "半分以上は今日か今週から始められる小さな行動にする",
      "続ける仕組み（記録・宣言・ごほうび・やめること）の行動も混ぜる",
      "「頑張る」「意識する」のような、やったかどうか分からない言葉にしない",
    ],
    offline: [
      "「{seed}」で今日やることを、1つだけ決めて書く",
      "「{seed}」の締め切りを、カレンダーに入れる",
      "「{seed}」の進み具合を、数字で測る方法を決める",
      "毎日同じ時間・同じ場所で、5分だけ取り組む",
      "週に1回、進み具合をふり返る日を決める",
      "つまずきそうな場面と、その対策を先に1つ決める",
      "人に宣言して、1週間後に報告する約束をする",
      "達成できたときの、小さなごほうびを用意する",
      "最初の一歩を、5分でできる大きさに分ける",
      "目標のために、やめることを1つ決める",
    ],
  },
  review: {
    ask: "振り返りを次に生かす具体的な答え（続けること・やめること・次に試すこと）",
    length: [10, 30],
    rules: [
      "「続ける：」「やめる：」「次に試す：」のどれかで始め、その後に具体的な行動を書く（例: 「次に試す：朝いちばんに重い作業をやる」）",
      "3つの種類をまんべんなく混ぜる",
      "反省や感想で終わらせず、次に何をするかが分かるようにする",
      "自分を責める言い方はしない",
    ],
    offline: [
      "続ける：「{seed}」でうまくいったやり方",
      "続ける：毎週同じ曜日に振り返る",
      "続ける：助けてくれた人にお礼を伝える",
      "やめる：うまくいかなかった原因の1つ",
      "やめる：やらなくても困らなかったこと",
      "やめる：一人で抱え込むこと",
      "次に試す：学びを一言のルールにして残す",
      "次に試す：回数や時間を数字で記録する",
      "次に試す：来週の予定に1つだけ入れる",
      "次に試す：1か月前の自分と比べてみる",
    ],
  },
  learn: {
    ask: "理解が進む具体的な答え（要点、身近な例、よくある誤解、確かめ方、次に調べるキーワード）",
    length: [10, 32],
    rules: [
      "要点や例を、短い一文で言い切る（例: お題「円安」なら「輸入品の値段が上がりやすくなる」）",
      "要点・身近な例・よくある誤解・次に調べるキーワードを混ぜる",
      "自信のない事実は断定せず、「〜と言われる」「〜で確かめる」の形にする",
      "専門用語を使うときは、ひと言で言い換えを添える",
    ],
    offline: [
      "「{seed}」を、一言で説明してみる",
      "「{seed}」の身近な例を、3つ挙げてみる",
      "「{seed}」のよくある誤解を1つ調べる",
      "「{seed}」は公式の情報や一次資料で確かめる",
      "「{seed}」で分からない言葉を、3つだけ調べる",
      "「{seed}」の入門の本か動画を、1つ選んで見る",
      "「{seed}」の始まりや歴史を調べる",
      "「{seed}」を、似たものと比べてみる",
      "「{seed}」を人に5分で説明してみる",
      "「{seed}」に詳しい人の解説を聞く",
    ],
  },
};

/** AI への指示（「具体的にする」用。お題・話の流れ・重複しない語は gemini-core が渡す） */
export function buildDetailPrompt(mode: BoardMode, seed: string, count: number, flow: string, banned: string): string {
  const preset = DETAIL_PRESETS[mode] ?? DETAIL_PRESETS.chat;
  const [min, max] = preset.length ?? [12, DETAIL_LABEL_MAX];
  return `お題「${seed}」について、${preset.ask}をちょうど${count}個出してください。
${flow}
よい答え:
${preset.rules.map((rule) => `- ${rule}`).join("\n")}
- ${count}個が同じ方向に偏らず、それぞれ別の具体策・例になっている
${flow ? "- このカードは元のお題の中の1つの切り口。答えは、話の流れのいちばん最初のお題について、このカードの切り口に沿って出す\n" : ""}
条件:
- 日本語のみ
- 各要素は${min}〜${max}文字
- 文のときは、読みやすいように区切りに読点「、」を入れる（文末の「。」は付けない）
- 番号・箇条書き記号・引用符・かぎかっこ・Markdown・コードフェンスは付けない
- 次と重複しない: ${banned}

出力は JSON 配列だけ。要素はちょうど${count}個。前後に文字を付けない。`;
}

/**
 * キー無し・AI に届かないときの答え。順番は毎回少し入れ替える。
 * 雑談以外はカードが「具体的な場面」のような切り口なので、元のお題（context のいちばん遠い祖先）について答える
 */
export function mockDetailTopics(
  seed: string,
  existing: string[],
  count: number,
  mode: BoardMode = "chat",
  context: string[] = [],
  random: () => number = Math.random,
): string[] {
  const root = mode === "chat" ? undefined : context[context.length - 1];
  const label = (root ?? seed).trim().replace(/[「」]/g, "") || "このお題";
  const used = new Set(existing);
  const pool = (DETAIL_PRESETS[mode] ?? DETAIL_PRESETS.chat).offline
    .map((template) => template.replaceAll("{seed}", label))
    .filter((text) => !used.has(text));
  // 先頭の数個は残しつつ軽く混ぜる（毎回まったく同じにならないように）
  const shuffled = pool
    .map((text, index) => ({ text, order: index + random() * 3 }))
    .sort((a, b) => a.order - b.order)
    .map((item) => item.text);
  return shuffled.slice(0, count);
}

/**
 * 「具体的にする」の答えを図鑑のどのお題に記録するか。
 * 「一番つらい瞬間」のような汎用の切り口は元のお題しだいなので、話の流れの中の元のお題（無ければ記録しない）へ
 */
export function detailRecordSeed(seed: string, context: string[] = []): string | undefined {
  return isGenericAngle(seed) ? topicAnchor(context) : seed.trim() || undefined;
}
