import { DETAIL_LABEL_MAX } from "@/lib/constants";
import type { BoardMode } from "@/lib/types";

/**
 * 「具体的にする」: カードの切り口に対して、対応策・答え・話し方の例を短い文で 8 つ出す。
 * 展開の形（3×3）はふつうの広げ方と同じ。カードの文が長くなるだけ。
 */
export type DetailPreset = {
  /** AI に頼む中身 */
  ask: string;
  /** 1つの答えの文字数の目安（最小・最大） */
  length?: [number, number];
  rules: string[];
  /** キー無し・失敗したときの答え。{seed} をカードの文に置き換える */
  offline: string[];
};

export const DETAIL_PRESETS: Record<BoardMode, DetailPreset> = {
  chat: {
    ask: "このお題をもっと具体的にした話題（具体的なジャンル・もの・場面・エピソードの種類）",
    length: [6, 22],
    rules: [
      "体言止めの短い語句にする（例: お題「夜食」なら「深夜のカップ麺アレンジ」「コンビニの新作ホットスナック」）",
      "カテゴリの名前だけでなく、話がすぐ始められる具体的なもの・場面にする",
      "文や問いかけにしない",
    ],
    offline: [
      "{seed}の最近のエピソード",
      "{seed}の失敗談",
      "{seed}のあるある",
      "{seed}の昔と今",
      "{seed}の人に言えない本音",
      "{seed}のおすすめベスト3",
      "リスナーの{seed}体験",
      "{seed}のこだわり",
      "もしも{seed}がなかったら",
      "{seed}の初めての思い出",
    ],
  },
  advice: {
    ask: "相談への具体的な対応策（今日からできる行動、考え方の切り替え、気持ちの整え方、人への頼り方）",
    length: [10, 36],
    rules: [
      "メモの箇条書きのように、「〜してみる」「〜を試す」「〜と決める」の言い切りで終える",
      "「〜しよう」「〜はどう？」「〜だよ」のような語りかけ・励ましの言い方はしない",
      "お題の言葉を毎回くり返さず、行動そのものを具体的に書く（何を・いつ・どれだけ）",
      "説教・断定・診断はしない。本人を責めない",
      "命や健康に関わる深刻な内容なら、1つは「信頼できる人や専門の窓口に話す」を入れる",
    ],
    offline: [
      "「{seed}」について、思っていることを紙に3行だけ書き出してみる",
      "信頼できる人に、一言だけ話してみる",
      "考えるのは1日10分まで、と時間を決める",
      "自分で変えられることを、1つだけ選んで試す",
      "少し楽だった日の共通点を、手帳から探す",
      "今週中に答えを出さなくていい、と決めておく",
      "友達が同じことで悩んでいたら、何と声をかけるか考える",
      "まず睡眠と食事を、いつもより少し整える",
      "つらさが続くなら、専門の相談窓口に話してみる",
      "小さな一歩を、明日の予定に書き込む",
    ],
  },
  idea: {
    ask: "アイデアを形にする具体案（企画の中身・進め方・最初の一歩・盛り上げる工夫）",
    rules: ["そのまま試せる具体的な一文にする", "誰に・何を・どうやってが分かるようにする"],
    offline: [
      "「{seed}」を、10分で試せるいちばん小さな形にしてみる",
      "「{seed}」を、誰に向けるかを1人に絞って考え直す",
      "「{seed}」に、真逆の要素を1つ足してみる",
      "「{seed}」の似た例を3つ集めて、違いを書き出す",
      "「{seed}」を、1枚の告知文にまとめてみる",
      "「{seed}」の最初の3分で何をするかを決める",
      "「{seed}」に必要なもの・人・時間を書き出す",
      "「{seed}」の案をいくつか出して、リスナーに投票で選んでもらう",
      "「{seed}」がうまくいかないパターンを先に3つ考えておく",
      "「{seed}」を来週の予定に入れて、1回やってみる",
    ],
  },
  goal: {
    ask: "目標に近づく具体的な行動（いつ・何を・どれだけ・どう続けるか）",
    rules: ["いつ・何を・どれだけが分かる一文にする", "今日か今週から始められる小さな行動を中心にする"],
    offline: [
      "「{seed}」で今日やることを、1つだけ決めて書く",
      "「{seed}」の締め切りを、カレンダーに入れる",
      "「{seed}」ができたかどうかを、数字で測る方法を決める",
      "「{seed}」は、毎日同じ時間・同じ場所でやる",
      "「{seed}」の進み具合を、週に1回ふり返る日を決める",
      "「{seed}」でつまずきそうな場面と、その対策を先に決める",
      "「{seed}」を人に宣言して、報告する約束をする",
      "「{seed}」ができたときの、小さなごほうびを用意する",
      "「{seed}」を、5分でできる最初の一歩に分ける",
      "「{seed}」のために、やめることを1つ決める",
    ],
  },
  review: {
    ask: "振り返りを次に生かす具体的な行動や気づき（続けること・やめること・次に試すこと）",
    rules: ["次に何をするかが分かる一文にする", "よかった点と改善点の両方を入れる"],
    offline: [
      "「{seed}」でよかったことを、3つ具体的に書き出す",
      "「{seed}」で、次も続けたいことを1つ決める",
      "「{seed}」がうまくいかなかった原因を、1つに絞る",
      "「{seed}」を受けて次に試すことを、来週の予定に入れる",
      "「{seed}」を、回数や時間などの数字で振り返ってみる",
      "「{seed}」で助けてくれた人に、お礼を伝える",
      "「{seed}」の学びを、一言のルールにして残す",
      "「{seed}」を、1か月前の自分と比べてみる",
      "「{seed}」で、やめてよかったことも書き残す",
      "「{seed}」の振り返りを、毎週同じ曜日にやる",
    ],
  },
  learn: {
    ask: "具体的な答え・例・調べ方（要点、身近な例、確かめる方法、次に読むもの）",
    rules: [
      "要点や例を、短い一文で言い切る",
      "自信のない事実は断定せず、確かめ方を添える",
    ],
    offline: [
      "「{seed}」の入門の本か動画を、1つだけ選んで見てみる",
      "「{seed}」を、一言で説明するとどうなるか書いてみる",
      "「{seed}」について、よくある誤解を1つ調べてみる",
      "「{seed}」の具体例を3つ集めて比べてみる",
      "「{seed}」を、人に5分で説明してみる",
      "「{seed}」は、公式の情報や一次資料で確かめる",
      "「{seed}」で分からない言葉を、3つだけ調べる",
      "「{seed}」の始まりや歴史を調べてみる",
      "「{seed}」を、実際に小さく試してみる",
      "「{seed}」に詳しい人の話や解説を聞いてみる",
    ],
  },
};

/** AI への指示（「具体的にする」用。お題・話の流れ・重複しない語は gemini-core が渡す） */
export function buildDetailPrompt(mode: BoardMode, seed: string, count: number, flow: string, banned: string): string {
  const preset = DETAIL_PRESETS[mode] ?? DETAIL_PRESETS.chat;
  const [min, max] = preset.length ?? [12, DETAIL_LABEL_MAX];
  return `お題「${seed}」について、${preset.ask}をちょうど${count}個出してください。
${flow}
よい答え:
${preset.rules.map((rule) => `- ${rule}`).join("\n")}
- ${count}個が同じ方向に偏らず、それぞれ別の具体策・例になっている

条件:
- 日本語のみ
- 各要素は${min}〜${max}文字
- 文のときは、読みやすいように区切りに読点「、」を入れる（文末の「。」は付けない）
- 番号・箇条書き記号・引用符・かぎかっこ・Markdown・コードフェンスは付けない
- 次と重複しない: ${banned}

出力は JSON 配列だけ。要素はちょうど${count}個。前後に文字を付けない。`;
}

/**
 * キー無し・AI に届かないときの答え。順番は毎回少し入れ替える。
 * 雑談以外はカードが「具体的な場面」のような切り口なので、元のお題（context のいちばん遠い祖先）について答える
 */
export function mockDetailTopics(
  seed: string,
  existing: string[],
  count: number,
  mode: BoardMode = "chat",
  context: string[] = [],
  random: () => number = Math.random,
): string[] {
  const root = mode === "chat" ? undefined : context[context.length - 1];
  const label = (root ?? seed).trim().replace(/[「」]/g, "") || "このお題";
  const used = new Set(existing);
  const pool = (DETAIL_PRESETS[mode] ?? DETAIL_PRESETS.chat).offline
    .map((template) => template.replaceAll("{seed}", label))
    .filter((text) => !used.has(text));
  // 先頭の数個は残しつつ軽く混ぜる（毎回まったく同じにならないように）
  const shuffled = pool
    .map((text, index) => ({ text, order: index + random() * 3 }))
    .sort((a, b) => a.order - b.order)
    .map((item) => item.text);
  return shuffled.slice(0, count);
}
